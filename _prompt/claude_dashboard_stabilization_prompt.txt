You are working inside an existing Node.js trading bot monorepo.

The trading engine (autopilot + order execution) is working correctly and placing trades. DO NOT break it.

Your task is to stabilize the dashboard + API layer and remove remaining architectural inconsistencies after a recent refactor to a canonical trade schema.

---

## CONTEXT

The system has recently been upgraded to use a canonical trade contract:

Canonical fields:
- strategyName
- stopLoss
- takeProfit
- quantity
- riskAmount
- tradeId

However, parts of the codebase still:
- read legacy fields (strategy, stop, target, qty, risk)
- use old storage modules instead of the canonical journal
- crash the frontend if env vars are missing

---

## GOAL

Stabilize the app without rewriting it by:

1. Preventing frontend crash on missing API env
2. Unifying dashboard to use canonical tradeJournal
3. Maintaining backward compatibility for old stored trades
4. Keeping autopilot + execution untouched

---

## TASKS

### 1. FIX FRONTEND API INITIALIZATION

File:
client/src/lib/api.js

Fix:

import axios from "axios";

const baseURL = import.meta.env.VITE_API_URL || "http://localhost:5000/api";

export const api = axios.create({ baseURL });

if (!import.meta.env.VITE_API_URL) {
  console.warn("Missing VITE_API_URL, using fallback http://localhost:5000/api");
}

---

### 2. UNIFY DASHBOARD DATA SOURCE

File:
src/server/routes/dashboard.js

Replace imports with:

import {
  getOpenTrades,
  getClosedTrades,
  getTradeEvents
} from "../../journal/tradeJournal.js";

---

### 3. NORMALIZE TRADE FIELDS

Use:

const normalizeTrade = (t) => ({
  tradeId: t.tradeId ?? t.id,
  strategyName: t.strategyName ?? t.strategy,
  stopLoss: t.stopLoss ?? t.stop,
  takeProfit: t.takeProfit ?? t.target,
  quantity: t.quantity ?? t.qty,
  riskAmount: t.riskAmount ?? t.risk,
  entryPrice: t.entryPrice,
  exitPrice: t.exitPrice,
  pnl: t.pnl,
  pnlPct: t.pnlPct,
  status: t.status,
  symbol: t.symbol,
  normalizedSymbol: t.normalizedSymbol,
  openedAt: t.openedAt,
  closedAt: t.closedAt,
  metrics: t.metrics ?? {}
});

---

### 4. PATCH POSITIONS ROUTE

File:
src/server/routes/positions.js

Replace legacy reads with:

t.strategyName ?? t.strategy
t.stopLoss ?? t.stop
t.takeProfit ?? t.target
t.quantity ?? t.qty

---

## DO NOT MODIFY

- src/autopilot.js
- src/execution/orderManager.js
- broker logic
- tradeJournal writes

---

## SUCCESS CRITERIA

- npm run dev works
- dashboard loads
- no crashes
- old + new trades display
