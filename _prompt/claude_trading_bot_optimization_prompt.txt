You are working inside an existing trading bot codebase. Do not redesign the whole project. Perform a focused optimization pass with minimal, safe refactors.

GOAL
Improve:
1. performance
2. UI responsiveness
3. database/storage cleanup
4. codebase consistency

CONTEXT
This bot is an automated trading system with:
- market scan
- strategy evaluation
- decision logging
- trade execution
- open position monitoring
- dashboard visibility

The intended system behavior includes:
- decision metrics such as close, breakout level, ATR, volume ratio, and distance-to-breakout
- open positions that merge broker position data with internal trade journal context
- activity feed visibility across the trade lifecycle
- recurring dashboard polling without unnecessary backend or broker load

WHAT I WANT YOU TO DO
Audit the current codebase and implement a practical optimization pass focused on the following priorities.

PRIORITY 1 — REMOVE ARCHITECTURAL DRIFT
Find and fix duplicate or overlapping runtime paths.
Specifically look for:
- multiple server entrypoints
- overlapping Alpaca client layers
- duplicate repository/data-access folders
- legacy JSON storage bridges still living beside active Mongo paths
- old scripts or boot paths still present in package.json

Requirements:
- choose one canonical server entrypoint
- choose one canonical Alpaca integration path
- choose one canonical repository/data layer
- remove or isolate legacy code paths that are no longer active
- do not break current working features

PRIORITY 2 — REDUCE DASHBOARD OVERFETCH
The dashboard should not poll many overlapping endpoints if the data is highly related.

Implement:
- a single aggregate endpoint for the main dashboard, such as:
  /api/dashboard/overview

This endpoint should return a single payload containing:
- status
- summary
- latestCycle
- decisions
- openPositions
- activity

Requirements:
- reduce repeated broker calls
- reduce repeated Mongo queries
- add a small safe in-memory cache if useful (for example 5–10 seconds)
- preserve correctness over cleverness

PRIORITY 3 — FIX HOT DATABASE PATHS
Avoid loading entire collections into memory and filtering in JavaScript when only recent or daily records are needed.

Implement repo/data-access improvements such as:
- getClosedTradesForDate(date)
- getTradeEventsForDate(date)
- getLatestOpenTrades(limit)
- getLatestDecisionsForDate(date, limit)

Requirements:
- query Mongo directly with filters, sort, and limit
- stop doing full-collection reads followed by in-memory filtering
- keep response shapes backward-compatible where reasonable

PRIORITY 4 — CLEAN UP SCHEMAS AND INDEXES
Normalize persistence for performance and consistency.

Requirements:
- use real Date fields for primary timestamps where possible
- keep dateKey or YYYY-MM-DD fields only if they clearly help daily queries
- add appropriate indexes for common reads

Recommended indexes:
Decision:
- { date: 1, timestamp: -1 }
- { date: 1, approved: 1, timestamp: -1 }
- { normalizedSymbol: 1, timestamp: -1 }

TradeEvent:
- { date: 1, timestamp: -1 }
- { tradeId: 1, timestamp: -1 }
- { symbol: 1, timestamp: -1 }

OpenTrade:
- { status: 1, updatedAt: -1 }
- { normalizedSymbol: 1, status: 1 }
- { brokerOrderId: 1 } sparse if needed

ClosedTrade:
- { closedAt: -1 }
- { symbol: 1, closedAt: -1 }

Also:
- identify fields currently stored as strings that should be Date
- make the smallest safe migration-oriented changes possible
- do not introduce destructive migrations unless necessary

PRIORITY 5 — HARDEN OPEN POSITION MERGING
Open positions should reflect:
broker data + internal journal/trade state

Requirements:
- match by brokerOrderId or tradeId first
- fall back to symbol matching only when needed
- reduce orphaned position ambiguity
- assume one active open trade per normalized symbol unless the current system explicitly supports pyramiding
- add guard logic or reconciliation where appropriate

PRIORITY 6 — NORMALIZE ACTIVITY FEED EVENTS
Find drift between backend-emitted event types and frontend styling or rendering maps.

Requirements:
- normalize event types
- make frontend and backend agree on the same event labels
- ensure common event types such as trade_opened, trade_closed, stop_loss_hit, take_profit_hit, failed, cycle_started, cycle_complete are rendered consistently

PRIORITY 7 — IMPROVE FRONTEND QUERY STRATEGY
Optimize the dashboard frontend without changing the core UX.

Requirements:
- use one main dashboard query for overview data
- reduce overlapping polling
- disable unnecessary refetch-on-focus if appropriate
- use sensible staleTime / refetchInterval values
- preserve a responsive UI
- avoid unnecessary rerenders and repeated transforms

PRIORITY 8 — STORAGE / RETENTION CLEANUP
Implement practical cleanup guards so the app does not degrade over time.

Requirements:
- cap activity feed reads to recent records
- avoid unbounded hot-path queries
- add retention or archive logic only if it fits cleanly into the current structure
- keep it simple and safe

CODING RULES
- make minimal, production-safe changes
- prefer improving the existing structure rather than rewriting everything
- keep API logic out of React components
- keep components focused on rendering
- do not hard-code localhost or production URLs in frontend services
- use environment variables consistently
- preserve current working features
- keep new code concise and maintainable

DELIVERABLES
I want the output in this exact structure:

1. AUDIT SUMMARY
- list the main performance issues you found
- list the main UI issues you found
- list the main database/storage issues you found

2. IMPLEMENTED CHANGES
- list every changed file
- explain what changed in each file
- explain why the change improves performance or cleanup

3. FULL UPDATED FILES
- provide the full contents of every changed file
- do not give partial snippets unless unchanged sections are truly irrelevant
- keep files copy-paste ready

4. OPTIONAL SAFE FOLLOW-UPS
- list only the next highest-value improvements that were intentionally not done yet

IMPORTANT
- Do not respond with generic advice only
- Actually perform the optimization pass against the existing code
- Prefer safe improvements over ambitious rewrites
- If you find dead or duplicate code, call it out clearly
- If something should not be changed yet because it risks breaking working behavior, say so explicitly
