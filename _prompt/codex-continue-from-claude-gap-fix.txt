Codex Prompt — Continue From Claude’s Partial Implementation and Close the Remaining Gaps

Continue from the CURRENT local repo state. Do not restart the project from scratch.

Claude Code already completed a substantial part of the trading bot fix pass. I need you to audit what is already implemented, identify the remaining gaps, and finish only the missing or incomplete parts cleanly.

==================================================
WHAT CLAUDE ALREADY IMPLEMENTED
==================================================

From the current repo state, these changes are already present:

1. Broker-sync / position enrichment work exists:
- `src/journal/positionEnricher.js`
- enriched metadata fields exist, including:
  - `origin`
  - `managementStatus`
  - `riskSource`
  - `exitCoverage`
  - `stopLoss`
  - `takeProfit`
  - `riskPerUnit`

2. Dashboard and UI wiring for management badges exists:
- `client/src/components/OpenPositionsTable.jsx`
- `client/src/components/OpenPositionsMobileList.jsx`

3. Journal separation is already fixed:
- broker_sync rows are excluded from the default Journal table
- Journal summary distinguishes:
  - `journalOpenTrades`
  - `brokerSyncOpenTrades`
  - `liveOpenPositions`

4. Breakout strategy work has already started:
- breakout classification exists
- `near_breakout` exists
- grouped rejection mapping exists
- new tests were added:
  - `tests/journal/positionEnricher.test.js`
  - `tests/strategies/breakoutClassification.test.js`

5. Rejection analytics work has already started:
- `src/repositories/analyticsRepo.mongo.js`
- `src/server/routes/rejections.js`
- `client/src/components/RejectionBreakdown.jsx`

==================================================
AUDIT FINDINGS — WHAT STILL LOOKS INCOMPLETE
==================================================

Based on the current codebase audit, these are the main remaining gaps to finish:

A. Derived / broker-sync riskAmount is still incomplete
- `positionEnricher.js` derives `riskPerUnit`, but not `riskAmount`
- `dashboard.js` currently sets:
  - `riskAmount: trade?.plannedRiskAmount ?? trade?.riskAmount ?? null`
- this means derived broker-sync positions can still show blank / misleading risk amount in the dashboard
- fix this by calculating riskAmount when possible:
  - `abs(quantity) * riskPerUnit`
  - and expose it in the dashboard payload
- do not leave derived broker-sync positions looking like they have no risk data if it can be computed

B. Dashboard still duplicates merge logic instead of using the canonical shared merge path
- `src/server/routes/dashboard.js` still has its own `buildOpenPositions(...)`
- `src/journal/tradeJournal.js` already has `mergeBrokerPositionsWithJournal(...)`
- consolidate so dashboard open-position routes and summary use the shared canonical merge/enrichment path instead of duplicated logic

C. Strategy-side work needs finishing / tightening
- breakout classification exists, but verify that score-based gating and clearer reason handling are actually complete and not just partially wired
- keep hard rejects for safety conditions
- ensure signal-quality reasons are explicit and useful

D. Rejection analytics should be verified end to end
- grouped categories and exact reasons exist in some places
- confirm dashboard / rejection API / frontend all use the richer analytics cleanly
- fill any missing mappings or display gaps

E. One pre-existing failing test was identified by Claude
- `GET /api/dashboard/decisions — pagination › returns newest-first ordering`
- Claude noted this was already failing before these changes because the dashboard route uses `.reverse()` instead of a true timestamp sort
- audit whether this is still failing
- fix it if it is cheap and isolated
- if you do fix it, keep the change minimal and safe

==================================================
YOUR TASK
==================================================

1. Inspect the current repo state first
- Read the files that Claude already changed
- Do not overwrite completed work unnecessarily
- Build on the current implementation

2. Finish the remaining implementation gaps:
- derived broker-sync `riskAmount`
- canonical live position merge reuse in dashboard
- any missing strategy/refinement glue
- any missing rejection analytics glue
- optionally fix the dashboard decisions newest-first ordering test if still failing and safe to address

3. Preserve all these constraints:
- do not break dashboard live positions
- do not break journal performance integrity
- do not reintroduce broker_sync rows into the default journal trade table
- do not silently count broker_sync rows as performance trades
- do not make unmanaged positions look safe
- do not do broad unrelated refactors

==================================================
REQUIRED IMPLEMENTATION DETAILS
==================================================

A. Derived broker-sync riskAmount
Update the canonical enrichment / merge path so broker-sync positions can expose a computed `riskAmount` when `riskPerUnit` and quantity are known.

Expected behavior:
- if managementStatus is `derived` and quantity exists:
  - `riskAmount = abs(quantity) * riskPerUnit`
- if managementStatus is `managed` and journal risk already exists, preserve journal risk
- if unmanaged and no derivation is possible:
  - keep risk null
  - do not pretend risk is 0

Places to inspect/update:
- `src/journal/positionEnricher.js`
- `src/journal/tradeJournal.js`
- `src/server/routes/dashboard.js`

B. Canonical live-position merge
Refactor dashboard routes so they reuse one canonical merged live-position builder instead of duplicating merge logic.

Likely direction:
- either enhance and reuse `mergeBrokerPositionsWithJournal(...)`
- or extract a shared helper/service used by both:
  - dashboard summary
  - dashboard open positions endpoint
  - any other open-position consumers

Goal:
one source of truth for:
- broker/journal merge
- origin
- managementStatus
- riskSource
- exitCoverage
- stopLoss / takeProfit / riskPerUnit / riskAmount
- orphaned / broker_sync semantics

C. Strategy quality / breakout logic validation
Audit `src/strategies/breakoutStrategy.js` and make sure the partial work is coherent:
- confirmed_breakout / near_breakout / no_breakout classification
- explicit reason labels
- score-based gating where intended
- no regression to unsafe approvals

If anything is partially wired, finish it cleanly.

D. Rejection analytics completion
Audit:
- `src/repositories/analyticsRepo.mongo.js`
- `src/server/routes/rejections.js`
- `client/src/components/RejectionBreakdown.jsx`

Make sure:
- exact reasons are included cleanly
- grouped categories are consistent
- top reasons render properly
- no missing mappings / broken labels remain

E. Dashboard newest-first ordering test
Check the decisions route ordering behavior.
If the test is still failing and the fix is isolated:
- replace reverse-based ordering with explicit timestamp-desc sorting
- keep the change minimal
- only fix if safe and clearly scoped

==================================================
TESTING
==================================================

Run targeted tests first.

At minimum, run the relevant ones for:
- `tests/journal/positionEnricher.test.js`
- `tests/strategies/breakoutClassification.test.js`
- `tests/server/dashboardRoutes.test.js`
- `tests/server/journalRoutes.test.js`
- `tests/exit/exitEngine.test.js`
- any rejection analytics tests if they exist

If the project dependencies are not installed in this environment, install only what is necessary and continue.

When reporting results:
- clearly distinguish:
  - passing tests
  - pre-existing failures
  - new failures introduced by your changes

==================================================
OUTPUT FORMAT
==================================================

When done, respond with:
1. what Claude had already completed
2. what you finished
3. changed files
4. test results
5. any remaining blockers or follow-up items

Implement the missing work directly. Do not just describe the plan.
