You are working inside my existing trading bot repo. Implement the trailing stop optimization completely and safely.

## Goal
Add ATR-based trailing stop behavior using this env var:

TRAILING_ATR_MULTIPLIER=1.5

The bot should use this multiplier to trail profitable open positions in a controlled way that improves downside protection without breaking the current stop-loss / take-profit flow.

## Important constraints
- Do not do a broad rewrite.
- Follow the existing repo structure and conventions.
- Keep changes minimal but production-ready.
- Preserve existing behavior unless required for trailing-stop support.
- Keep API logic out of UI components.
- Handle all async failures explicitly.
- Add or update tests for backend behavior.
- Update `.env.example` only if needed.
- Do not hard-code config values; read them from the existing config/env flow.
- Return full updated files only for files that change.

## What to implement

### 1. Env/config support
Wire `TRAILING_ATR_MULTIPLIER` into the backend config layer.

Requirements:
- Read it from environment/config in the same style as the rest of the repo.
- Validate it as a positive number.
- Make it available to the exit engine / position monitoring logic.
- Use `1.5` in `.env.example`.

### 2. Exit engine trailing-stop logic
Inspect the current exit engine and position monitor flow and implement ATR-based trailing stop support.

Expected behavior:
- Trailing logic should only apply once a trade is sufficiently in profit, not immediately at entry.
- Use the trade direction and ATR to calculate a tighter protective stop as price moves favorably.
- For long trades, the trailing stop should move upward only, never downward.
- Never loosen risk.
- Never move the stop in a way that makes it worse than the existing stop.
- Use `TRAILING_ATR_MULTIPLIER * ATR` as the trail distance basis.
- If ATR is unavailable for a position, fail safely and keep current stop behavior unchanged.
- Keep current hard-stop / take-profit logic intact unless the repo already intends trailing to replace a static target.
- Prefer updating stop state through the journal/state system if that already exists in the repo.

### 3. Position monitor integration
Update the position monitoring flow so that:
- trailing-stop evaluation happens during open-position monitoring
- any stop adjustment is persisted properly
- exit checks continue to work correctly after stop adjustments
- activity / trade events reflect trailing updates where appropriate

### 4. Persistence / journal updates
If the repo has trade journal or trade event persistence:
- record stop updates caused by trailing
- use a clear event/reason such as `stop_trailed`
- preserve auditability of old vs new stop values if the current event model supports it

### 5. API/dashboard support
If the backend currently returns open-position stop data to the dashboard:
- make sure the updated trailing stop value is surfaced correctly
- do not add unnecessary frontend changes unless the current UI already displays stop values
- if a small backend response shape update is needed, do it cleanly

### 6. Tests
Add/update backend tests to verify:
- `TRAILING_ATR_MULTIPLIER` is read correctly from config
- trailing stop for long positions moves up when price advances enough
- trailing stop never moves backward
- no trailing update happens when ATR is missing
- existing stop-loss / take-profit behavior still works
- trade/journal event is recorded when stop is trailed, if the repo supports that

## Implementation guidance
Inspect and update the actual files that already own this behavior. Likely areas include:
- config/env loader
- exit engine
- position monitor
- trade journal / trade events
- related tests

Use the repo’s existing naming and architecture. Do not invent a parallel system if one already exists.

## Output format
Give me:
1. a short changed-files list
2. full updated contents for every changed file
3. any new test files in full
4. a short note on how the trailing stop now behaves

## Success criteria
The repo should support:
- TRAILING_ATR_MULTIPLIER=1.5
- ATR-based trailing stop updates for profitable positions
- safe monotonic stop movement
- preserved journal/audit behavior
- passing tests
